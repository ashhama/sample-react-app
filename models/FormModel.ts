class FormItem {
  id: string;
  title: string;
  href: string;

  constructor(id:string, title: string, href: string) {
    this.id = id;
    this.title = title;
    this.href = href;
  }
}

export default FormItem;
